#include <stdio.h>


enum option
{
	n = 0,
	i,
	t
};


int main(int argc, char* argv[])
{
	if (argc > 1)
	{
		printf("Option %s\n", argv[1]);
	}
	else
	{
		printf("Usage: hello <option>\n");
	}

	return 0;
}
